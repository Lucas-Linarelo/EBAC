const campoNome = document.getElementById('nome');
const fomulário = document.getElementsByTagName('form');
const botaoEnviar = document.getElementById('btn-enviar');

botaoEnviar?.addEventListener('click', (e:MouseEvent)=>{
    e.pageY
});